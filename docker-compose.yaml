services:
    chessdocs-api:
        container_name: chessdocs-api
        image: chessdocs-api
        build: .
        env_file: .env
        environment:
            PORT: 8787
        ports:
            - "8787:8787"
        restart: unless-stopped
